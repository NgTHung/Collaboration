<style>
  :root {
    --aurora: repeating-linear-gradient( 100deg, #60a5fa 10%, #e879f9 16%, #5eead4 22%, #60a5fa 30%);
    --strip-dark: repeating-linear-gradient(100deg, #000 0%, #000 7%, transparent 10%, transparent 12%, #000 16%);
    --bg-primary: #101728;
  }
  body {
    margin: 0;
    box-sizing: border-box;
    background-color: var(--bg-primary) !important;
  }

  .aurora {
    position: fixed;
    top: 0;

    background-image: var(--strip-dark), var(--aurora);
    filter: opacity(50%) saturate(200%);
    background-size: 300%, 200%;
    background-position: 50% 50%, 50% 50%;
    opacity: 0.3;
    height: inherit;

    contain: strict;
    contain-intrinsic-size: 100vw 80vh;
    mask-image: radial-gradient(ellipse at 100% 0%, var(--bg-primary) 40%, transparent 70%);
  }
  .aurora::after {
    content: "";
    position: absolute;
    top: 0;
    right: 0;
    bottom: 0;
    left: 0;
    background-size: 200%, 100%;
    mix-blend-mode: difference;
    background-image: var(--strip-dark), var(--aurora);
    animation: aurora 90s linear infinite;
  }
  @keyframes aurora {
      0% {
      background-position: 50% 50%, 50% 50%;
    }
    100% {
      background-position: 350% 50%, 350% 50%;
    }
  }

  .content {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    height: 80vh;
    opacity: 0.8;

    img {
      width: 10rem;
      border-radius: 100rem;
    }
  }
</style>

<body>
  <div class="aurora"></div>
  <div class="content">
    <h1>Baobao</h1>
    <p>Just a aurora effect I found at elysiajs.com</p>
    <img src='https://avatars.githubusercontent.com/u/115572050?v=4'/>
  </div>
</body>